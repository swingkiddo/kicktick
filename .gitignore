# Dependencies
node_modules/
npm-debug.log*

# Build outputs
dist/
build/
.next/
target/
*.tsbuildinfo

# Env / secrets
.env
.env.local
*.pem

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Anchor / Solana
.anchor/
*.so
deployment-*.json

# Deployer keypairs (keep local only, never commit)
*-deployer.json
*-keypair.json

# Logs
*.log

skills-lock.json
opencode.json

docs/*.md
docs/*.pdf
docs/superpowers
.agents
kicktick/test-ledger
test_scripts
keypair.json
test-ledger/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
